{
  "name": "the-truth-machine",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "npm run validate:content && next build",
    "start": "next start",
    "lint": "next lint",
    "test": "node --test tests/*.test.mjs",
    "validate:content": "node scripts/validate-content.mjs && node scripts/audit-tree-data.mjs",
    "check": "npm run validate:content && npm test && npm run lint && npm run build",
    "build:tree-data": "node scripts/build-tree-data-strict.mjs"
  },
  "dependencies": {
    "d3-hierarchy": "^3.1.2",
    "next": "14.2.5",
    "react": "18.3.1",
    "react-dom": "18.3.1"
  }
}
